{
  "name": "vsa-recoverability-atlas",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.14-bookworm",
  "postCreateCommand": "python -m pip install --upgrade pip && python -m pip install -e \".[dev,publication,competitors,level3]\" && python scripts/validate_evidence_registry.py",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  }
}
